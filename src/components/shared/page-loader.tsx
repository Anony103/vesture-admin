import { ClipLoader } from "react-spinners";

export const PageLoader = () => (
  <div className="min-h-[30vh] flex items-center justify-center">
    <ClipLoader color="#7C2EBF" size={40} />
  </div>
);
