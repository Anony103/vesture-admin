import { ReactElement } from 'react'

interface Props {
    
}

function HeaderCard({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default HeaderCard
